# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# scratch locale delle sessioni Claude/Cowork — non è codice del progetto,
# e "Claude outputs/env.local" (senza il punto iniziale: NON combacia con
# ".env*" qui sopra) è quasi finito nel commit di Fase 3 con dentro
# DATABASE_URL, CRON_SECRET e le chiavi API vere. Intercettato prima del
# commit, mai stato in git — ma la cartella va esclusa per intero.
/Claude outputs/
